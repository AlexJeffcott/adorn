{
	"name": "frontend-react",
	"version": "1.0.2",
	"license": "MIT",
	"scripts": {
		"//base_scripts": "the building blocks of commandscripts go here",
		"clean": "rm -rf build",
		"start": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"types": "tsc",
		"eslint": "eslint --ignore-path .gitignore --fix .",
		"prettier": "prettier --ignore-path .gitignore --write .",
		"jscpd": "jscpd",
		"//command_scripts": "the compositions of base_scripts go here",
		"lint": "npm run types && npm run prettier && npm run eslint && npm run jscpd",
		"test": "wtr **/*.test.tsx --node-resolve --playwright --browsers chromium firefox"
	},
	"dependencies": {
		"annotate": "*",
		"react": "^18.0.0",
		"react-dom": "^18.0.0",
		"react-router-dom": "^6.3.0",
		"test-utils": "^1.0.2"
	}
}
